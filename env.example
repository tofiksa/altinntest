# Altinn/ID-porten OAuth Configuration
# Get these values from Altinn documentation or your Altinn application registration

# OAuth Client Configuration
CLIENT_ID=e6613804-f2ef-4a45-ba9a-f0775b805e11
CLIENT_SECRET=6b9cff2d-bcda-47bf-9677-06bd13375862

# ID-porten OpenID Connect Discovery
# The application will automatically discover authorization, token, and userinfo endpoints
IDPORTEN_DISCOVERY_URL=https://test.idporten.no/.well-known/openid-configuration

# Alternative: Override individual endpoints if needed (not recommended)
# If these are set, they will override the discovered endpoints
# AUTHORIZATION_URL=https://test.idporten.no/authorize
# TOKEN_URL=https://test.idporten.no/token
# USERINFO_URL=https://test.idporten.no/userinfo

# Altinn Platform API
ALTINN_PLATFORM_URL=https://platform.altinn.no

# Altinn App API Configuration
# For App API: https://{org}.apps.altinn.no/{org}/{appname}
ALTINN_ORG=your-org-name
ALTINN_APP_NAME=your-app-name

# Application Configuration
PORT=3000
SESSION_SECRET=your-secret-key-change-in-production
BASE_URL=http://localhost:3000

# OAuth Scopes (space-separated)
# Common scopes: openid, profile, altinn:instances.read, altinn:apps.read, altinn:instances.write
OAUTH_SCOPES=openid profile altinn:instances.read altinn:apps.read

# RAR (Rich Authorization Requests) for Ansattporten
# Optional: JSON array of authorization_details to request representation relationships
# See: https://docs.digdir.no/docs/ansattporten/ansattporten_rar.html
# Example for Altinn 3 resource:
# RAR_AUTHORIZATION_DETAILS=[{"type":"ansattporten:altinn:resource","resource":"urn:altinn:resource:your-resource-id"}]
# Example for organization number:
# RAR_AUTHORIZATION_DETAILS=[{"type":"ansattporten:orgno"}]
# RAR_AUTHORIZATION_DETAILS=

